The opt-in shared browser now stays off when its profile or state directory is on network, FUSE or synced storage such as iCloud, OneDrive or Dropbox, and that server drives its own browser with one warning.
