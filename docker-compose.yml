services:
  linkedin-mcp:
    image: linkedin-mcp-server:local
    restart: unless-stopped
    volumes:
      - ~/.linkedin-mcp:/home/pwuser/.linkedin-mcp
    environment:
      - LOG_LEVEL=WARNING
    stdin_open: true
    tty: true
